import React, { useState } from 'react';
import { Layout } from '@gatsby-themes/core';
import { LandingPage } from '@gatsby-themes/tailwind';

const IndexPage = () => {
	return (
		<Layout>
			<LandingPage />
		</Layout>
	);
};

export default IndexPage;
